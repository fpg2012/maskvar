[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "maskvar"
version = "0.1.0"
description = "MaskVar package"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["maskvar*"]
